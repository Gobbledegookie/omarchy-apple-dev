# mise #13289 / #13291 retest, 2026-09-17

Host: jwm1-linux, Omarchy 4.0.1rc2 aarch64 (`ID=omarchy`, `ID_LIKE=arch`).
System mise: 2026.8.8 (`mise-bin` 2026.8.8-1).
Retest binary: cargo install from jdx/mise `533346cc` (merge of #13297,
stacked on #13293). Crate still reports `2026.9.10 linux-arm64 (2026-09-17)`.
Isolated `MISE_*_DIR` under `~/.cache/mise-retest`. `TMPDIR=$HOME/tmp`.

Latest tagged release at retest: `v2026.9.10` published 2026-09-16 17:25Z,
before both merges. No tagged release contains the fixes yet.

## 13289 (fabricated platform name)

Control, system 2026.8.8:

```
mise swift@6.3.3  [1/3] download swift-6.3.3-RELEASE-omarchy4.0.1rc2-aarch64.tar.gz
HTTP 404
```

Git build:

```
WARN  swift 6.3.3 publishes no build for omarchy 4.0.1rc2; using ubi9
```

Fixed on main.

## 13291 (arm64 directory suffix)

Git build then fetched:

```
https://download.swift.org/swift-6.3.3-release/ubi9-aarch64/swift-6.3.3-RELEASE/swift-6.3.3-RELEASE-ubi9-aarch64.tar.gz
```

Extract: 2009 files. Fixed on main.

## Remaining: ubi9 Swift does not run on Arch

Post-install `swift --version`:

```
error while loading shared libraries: libncurses.so.6
```

Host has `ncurses 6.6-2` with `libncursesw.so.6` only.
`/usr/lib/libncurses.so.6` is absent. mise rolled the install back.

This is the third issue jdx asked confirmed in #13289. Not filed unless
Joshua says to.

Practical: AUR `swift-bin` remains the path this repo installs.
